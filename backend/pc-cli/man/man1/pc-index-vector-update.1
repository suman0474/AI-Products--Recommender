.nh
.TH "PC" "1" "Dec 2025" "Pinecone CLI" "Pinecone CLI Manual"

.SH NAME
.PP
pc-index-vector-update - Update a vector by ID, or a set of vectors by metadata filter


.SH SYNOPSIS
.PP
\fBpc index vector update [flags]\fP


.SH DESCRIPTION
.PP
Update a single vector by ID (values, sparse values, metadata) or update metadata for all vectors matching a metadata filter.

.PP
Use --dry-run with --filter updates to preview how many records would be updated.
JSON inputs may be inline, ./file.json[l], or read from stdin with '-'.
A --body payload can supply id, values, sparse_values, metadata, filter, and dry_run fields.


.SH OPTIONS
.PP
\fB--body\fP=""
	request body JSON (inline, ./path.json, or '-' for stdin; only one argument may use stdin)

.PP
\fB--dry-run\fP[=false]
	do not update the vectors, just return the number of vectors that would be updated

.PP
\fB--filter\fP=
	filter to update the vectors with (inline JSON, ./path.json, or '-' for stdin)

.PP
\fB-h\fP, \fB--help\fP[=false]
	help for update

.PP
\fB--id\fP=""
	ID of the vector to update

.PP
\fB-n\fP, \fB--index-name\fP=""
	name of the index to update

.PP
\fB-j\fP, \fB--json\fP[=false]
	output as JSON

.PP
\fB--metadata\fP=
	metadata to update the vector with (inline JSON, ./path.json, or '-' for stdin)

.PP
\fB--namespace\fP="\fBdefault\fP"
	namespace to update the vector in

.PP
\fB--sparse-indices\fP=
	sparse indices to update the vector with (inline JSON array, ./path.json, or '-' for stdin)

.PP
\fB--sparse-values\fP=
	sparse values to update the vector with (inline JSON array, ./path.json, or '-' for stdin)

.PP
\fB--values\fP=
	values to update the vector with (inline JSON array, ./path.json, or '-' for stdin)


.SH OPTIONS INHERITED FROM PARENT COMMANDS
.PP
\fB-q\fP, \fB--quiet\fP[=false]
	suppress output

.PP
\fB--timeout\fP=1m0s
	timeout for commands, defaults to 60s (0 to disable)


.SH EXAMPLE
.EX
  $ pc index vector update --index-name my-index --id doc-123 --values '[0.1, 0.2, 0.3]'
  $ pc index vector update --index-name my-index --id doc-123 --sparse-indices ./indices.json --sparse-values ./values.json
  $ pc index vector update --index-name my-index --id doc-123 --metadata '{"genre":"sci-fi"}'
  $ pc index vector update --index-name my-index --filter '{"genre":{"$eq":"sci-fi"}}' --metadata '{"genre":"fantasy"}' --dry-run
  $ pc index vector update --index-name my-index --body ./update.json
  $ cat update.json | pc index vector update --index-name my-index --body -

.EE


.SH SEE ALSO
.PP
\fBpc-index-vector(1)\fP


.SH HISTORY
.PP
19-Dec-2025 Auto generated by spf13/cobra
