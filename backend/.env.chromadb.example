# =============================================================================
# VECTOR STORE CONFIGURATION
# =============================================================================
# Copy this file to .env and configure your preferred backend

# Choose vector store backend:
#   "faiss"          - Local FAISS (default, no setup needed)
#   "chromadb"       - Self-hosted ChromaDB server (Docker)
#   "chromadb_cloud" - ChromaDB Cloud (managed service)
VECTOR_STORE_TYPE=faiss

# =============================================================================
# OPTION A: LOCAL FAISS (Default)
# =============================================================================
# No additional configuration needed, data stored in:
VECTOR_STORE_DIR=./vector_store_data

# =============================================================================
# OPTION B: CHROMADB CLOUD (Managed Service)
# =============================================================================
# Sign up at: https://trychroma.com/
# Get your API key from the dashboard after creating a database
#
# VECTOR_STORE_TYPE=chromadb_cloud
# CHROMADB_CLOUD_API_KEY=your-api-key-here
# CHROMADB_CLOUD_TENANT=default_tenant
# CHROMADB_CLOUD_DATABASE=default_database

# =============================================================================
# OPTION D: SELF-HOSTED CHROMADB (Docker)
# =============================================================================
# Start server: docker-compose -f docker-compose.chromadb.yml up -d
#
# VECTOR_STORE_TYPE=chromadb
# CHROMADB_HOST=localhost
# CHROMADB_PORT=8000
# CHROMADB_AUTH_TOKEN=  # Optional, leave empty if no auth

# =============================================================================
# GOOGLE API (Required for embeddings - all backends)
# =============================================================================
GOOGLE_API_KEY=your-google-api-key
